
version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run:
          name: build
          command: |
            cmake --configure .
            cmake --build .
